# Meeting Transcriber does not need any environment variables.
#
# Your AssemblyAI API key is NOT set here. You enter it in the app under
# Settings, and it is stored encrypted on your device using the OS keychain
# (Electron safeStorage). It is never written into the project or this repo.
#
# This file exists only to document that there is no required env config.
